# Core runtime dependencies
mysql-connector-python==9.6.0
openpyxl==3.1.5
tkcalendar==1.6.1
Pillow==12.2.0
python-dotenv>=1.0.0
fpdf2>=2.8.0

# Development / testing (optional — not needed to run the app)
pytest>=8.0.0
ruff>=0.4.0
black>=24.0.0
